<section class="cta-section">
  <div class="cta-inner">

    <div class="cta-eyebrow reveal">Start a Conversation</div>
    <h2 class="cta-headline reveal">
      What Can We<br>
      <span class="accent">Build for You?</span>
    </h2>
    <p class="cta-sub reveal">New project, replacement parts, repair, or refurbishment — our engineers are ready to respond within 24 hours.</p>

    <div class="cta-actions reveal">
      <a href="/contact" class="btn-primary">
        Request a Quote
        <svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
      </a>
      <a href="/products" class="btn-secondary">View Products →</a>
    </div>

  </div>
</section>

<style>
  /* ── Section — dark navy gradient, closes the page ── */
  .cta-section {
    position: relative;
    overflow: hidden;
    text-align: center;
    padding-block: clamp(64px, 9vh, 104px);
    background:
      radial-gradient(ellipse 100% 60% at 20% -10%, rgba(140,180,255,.14) 0%, transparent 55%),
      radial-gradient(ellipse 60%  80% at 90% 110%, rgba(8,56,146,.25) 0%, transparent 55%),
      linear-gradient(145deg, #0e2454 0%, #163060 50%, #1a3875 100%);
  }
  .cta-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse 60% 80% at 50% 110%, rgba(192,24,30,.1), transparent 60%);
    pointer-events: none;
  }
  .cta-section::after {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(192,24,30,.3) 50%, transparent);
  }

  /* ── Container ── */
  .cta-inner {
    position: relative;
    z-index: 1;
    width: 100%;
    max-width: 1440px;
    margin-inline: auto;
    padding-inline: 16px;
    box-sizing: border-box;
  }
  @media (min-width: 480px)  { .cta-inner { padding-inline: 20px; } }
  @media (min-width: 768px)  { .cta-inner { padding-inline: 32px; } }
  @media (min-width: 1024px) { .cta-inner { padding-inline: 48px; } }

  /* ── Eyebrow ── */
  .cta-eyebrow {
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: .2em;
    text-transform: uppercase;
    color: var(--crimson-l);
    margin-bottom: 14px;
  }

  /* ── Headline ── */
  .cta-headline {
    font-family: var(--font-head);
    font-weight: 800;
    font-size: clamp(34px, 5vw, 64px);
    text-transform: uppercase;
    line-height: .97;
    color: var(--white);
    margin-bottom: 14px;
  }
  .cta-headline .accent { color: var(--crimson-l); }

  /* ── Sub copy ── */
  .cta-sub {
    font-family: var(--font-body);
    font-size: clamp(14px, 1.5vw, 16px);
    font-weight: 300;
    color: var(--fog);
    line-height: 1.65;
    max-width: 520px;
    margin-inline: auto;
    margin-bottom: 36px;
  }

  /* ── Buttons — matches index.astro ── */
  .cta-actions {
    display: flex;
    justify-content: center;
    gap: 14px;
    flex-wrap: wrap;
  }
  .btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: var(--crimson);
    color: var(--white);
    font-family: var(--font-head);
    font-weight: 700;
    font-size: 15px;
    letter-spacing: .1em;
    text-transform: uppercase;
    text-decoration: none;
    padding: 15px 36px;
    clip-path: polygon(10px 0%, 100% 0%, calc(100% - 10px) 100%, 0% 100%);
    transition: background .2s, transform .2s;
    white-space: nowrap;
  }
  .btn-primary:hover { background: var(--crimson-l); transform: translateX(2px); }

  .btn-secondary {
    display: inline-flex;
    align-items: center;
    gap: 9px;
    background: transparent;
    color: var(--chrome);
    font-family: var(--font-head);
    font-weight: 600;
    font-size: 15px;
    letter-spacing: .1em;
    text-transform: uppercase;
    text-decoration: none;
    padding: 14px 36px;
    border: 1px solid rgba(208,212,220,.2);
    transition: color .2s, border-color .2s;
    white-space: nowrap;
  }
  .btn-secondary:hover { color: var(--white); border-color: rgba(208,212,220,.5); }
</style>