<section class="sph-section">
  <div class="sph-inner">

    <div class="breadcrumb">
      <a href="/">Home</a>
      <span class="sep">/</span>
      <span class="current">Services</span>
    </div>

    <div class="page-eyebrow">
      <div class="page-eyebrow-line"></div>
      <span>End-to-End Capability</span>
    </div>

    <h1 class="page-title">
      From First<br>
      <span class="accent">Drawing</span> to<br>
      <span class="chrome">Last Bolt</span>
    </h1>

    <p class="page-sub">
      Jayveecons doesn't hand off. We design it, fabricate it, install it, and support it — from the initial engineering brief to long-term parts and maintenance, under one roof, under one team's accountability.
    </p>

  </div>
</section>

<style>
  /* ── Section shell — dark navy, matches ContactHero gradient ── */
  .sph-section {
    background:
      radial-gradient(ellipse 90% 70% at 65% 40%, rgba(8,56,146,0.9) 0%, transparent 60%),
      radial-gradient(ellipse 50% 60% at 5%  90%, rgba(192,24,30,.07) 0%, transparent 50%),
      linear-gradient(155deg, var(--navy-deep) 0%, var(--ink) 55%, var(--black) 100%);
    padding-block: clamp(100px, 14vh, 140px) clamp(48px, 7vh, 80px);
  }

  /* ── Container — same token as rest of site ── */
  .sph-inner {
    width: 100%;
    max-width: 1440px;
    margin-inline: auto;
    padding-inline: 16px;
    box-sizing: border-box;
  }
  @media (min-width: 480px)  { .sph-inner { padding-inline: 20px; } }
  @media (min-width: 768px)  { .sph-inner { padding-inline: 32px; } }
  @media (min-width: 1024px) { .sph-inner { padding-inline: 48px; } }

  /* ── Breadcrumb ── */
  .breadcrumb {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 28px;
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: .16em;
    text-transform: uppercase;
  }
  .breadcrumb a       { color: var(--mist); text-decoration: none; transition: color .2s; }
  .breadcrumb a:hover { color: var(--white); }
  .breadcrumb .sep    { color: var(--crimson); opacity: .6; }
  .breadcrumb .current { color: var(--crimson-l); }

  /* ── Eyebrow ── */
  .page-eyebrow {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 16px;
    animation: fadeUp .7s ease .1s both;
  }
  .page-eyebrow-line {
    width: 30px;
    height: 2px;
    background: var(--crimson);
    flex-shrink: 0;
  }
  .page-eyebrow span {
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: .2em;
    text-transform: uppercase;
    color: var(--crimson-l);
  }

  /* ── Headline ── */
  .page-title {
    font-family: var(--font-head);
    font-weight: 800;
    font-size: clamp(44px, 6.5vw, 84px);
    text-transform: uppercase;
    line-height: .93;
    letter-spacing: -.01em;
    color: var(--white);
    margin-bottom: 20px;
    animation: fadeUp .7s ease .2s both;
  }
  .page-title .accent {
    color: var(--crimson-l);
  }
  .page-title .chrome {
    background: linear-gradient(135deg, var(--chrome-l), var(--chrome));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* ── Sub copy ── */
  .page-sub {
    font-family: var(--font-body);
    font-size: clamp(14px, 1.7vw, 17px);
    font-weight: 300;
    color: var(--fog);
    max-width: 640px;
    line-height: 1.75;
    animation: fadeUp .7s ease .3s both;
  }
</style>