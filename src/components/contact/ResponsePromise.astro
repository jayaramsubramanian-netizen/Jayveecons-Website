
<section class="response-promise">
          <div class="rp-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg></div>
          <div>
            <div class="rp-title">At Jayveecons Group we provide Solutions - not a Sales Pitch</div>
            <p class="rp-text">We will acknowledge your submission the same business day. For straightforward requests, a technical or commercial response typically follows within two to three business days. Projects requiring process calculations, equipment sizing, or site-specific design input may take a little longer — the response time reflects the depth of the answer.</p>
          </div>
</section>

<style>
.response-promise{background:rgba(13,26,58,.5);border:1px solid rgba(51,96,184,.2);padding:clamp(18px,2.5vw,26px);display:flex;gap:16px;align-items:flex-start}
.rp-icon {
  width: 36px;
  height: 36px;
  /* Light red background (10% opacity) */
  background: rgba(224, 36, 40, 0.1); 
  /* Solid red border */
  border: 1px solid #e02428;
  border-radius: 4px; /* Optional: adds a slight curve to the "box" */
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  /* Color of the icon inside if using currentColor */
  color: #22c55e; 
}
.rp-icon svg{width:18px;height:18px}
.rp-title{font-family:var(--font-head);font-weight:700;font-size:16px;text-transform:uppercase;letter-spacing:.05em;color:var(--white);margin-bottom:4px}
.rp-text{font-size:13px;font-weight:300;color: #9098b0;line-height:1.6}
</style>